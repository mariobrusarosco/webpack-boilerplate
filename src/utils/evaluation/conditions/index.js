export const lessOrEqualsOneHundred = id => id <= 100
export const betweenOneAndTwoHundread = id => id > 100 && id <= 200
export const albumIDIsOne = albumID => albumID === 1
export const albumdIdBetweenTwoAndFour = albumID => albumID >= 2 && albumID <= 4
export const evenNumber = input => input % 2 !== 0
export const validId = id => !!id
